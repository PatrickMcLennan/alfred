import React, { FC } from 'react';
import { Container } from '@mui/material';

export const Crypto: FC = () => {
  return <Container maxWidth="lg">This is the crypto page This is the crypto page</Container>;
};
