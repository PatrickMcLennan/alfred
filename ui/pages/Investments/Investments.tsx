import React, { FC } from 'react';
import { Container } from '@mui/material';

export const Investments: FC = () => {
  return <Container maxWidth="lg">This is the investments page This is the investments page</Container>;
};
